import 'package:flutter/material.dart';

class DnsScreen extends StatelessWidget {
  const DnsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('DNS Protection Screen'),
    );
  }
} 